# https://taskfile.dev

version: "3"

includes:
  bastion-sessions:
    taskfile: bastion-sessions/taskfile.yaml
  oci-oke:
    taskfile: configs/blacksd/oci-oke/Taskfile.yaml
    dir: configs/blacksd/oci-oke
  object-storage:
    taskfile: configs/blacksd/object-storage/Taskfile.yaml
    dir: configs/blacksd/object-storage
  # terragrunt:
  #   taskfile: tasks/Terragrunt.taskfile.yaml
  # dependencies:
  #   # dir: tasks/
  #   taskfile: tasks/Dependencies-{{ OS }}.taskfile.yaml

silent: true

tasks:
  default:
    cmds:
      - task -l
      # - |
      #   echo
      #   echo 'Please use the following env vars to change the default behavior:
      #     TG_REFRESH=true       # invalidate cache
      #     TG_YOLO=true          # auto-approve
      #   '
